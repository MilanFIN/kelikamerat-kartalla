import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kelikamerat.kartalla',
  appName: 'Kelikamerat Kartalla',
  webDir: 'dist'
};

export default config;
